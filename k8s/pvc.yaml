apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce  # Only one pod mounts this volume at a time (correct for PostgreSQL)
  resources:
    requests:
      storage: 5Gi   # Size of the database disk
